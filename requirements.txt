# --- graph ---
neo4j>=5.28,<6            # Bolt 5.x driver; HydraDB is driver-compatible
httpx>=0.27               # HTTP transport for /v1/graphs/{graph}/query

# --- core ---
pydantic>=2.7
pyyaml>=6.0
python-dotenv>=1.0
tenacity>=8.2             # retry around LLM + graph writes

# --- entity resolution ---
rapidfuzz>=3.9            # Jaro-Winkler, token ratios
metaphone>=0.6            # phonetic blocking key
unidecode>=1.3

# --- llm ---
# No SDK required: llm.py talks to OpenAI-compatible endpoints over httpx, so
# Gemini / Groq / Cerebras / OpenRouter / Ollama / Anthropic all work with just
# a key in .env.

# --- cli ---
rich>=13.7                # terminal rendering for the CLI

# --- api (optional; the CLI is the primary interface) ---
fastapi>=0.115
uvicorn[standard]>=0.30

# --- eval ---
numpy>=1.26
tabulate>=0.9
