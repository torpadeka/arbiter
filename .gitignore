# HydraDB local node data (object store, cache, auth token)
.hydradb-data/
# MinIO data if it was ever bind-mounted here (the stack uses a docker volume)
.minio-data/
# CLI subgraph exports
out.html
*.subgraph.html

# Corpus + derived artifacts — never commit the dataset
data/raw/
data/herb/
data/scale/
data/interim/
data/cache/
*.jsonl.gz

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
*.egg-info/

# Node / Vite
node_modules/
ui/node_modules/
ui/dist/
.next/
out/
dist/

# Env + secrets
.env
.env.local

# Eval outputs
eval/results/

# Induced schema and CLI state, both generated from whatever corpus is loaded
ontology/generated.yaml
.arbiter/

# OS
.DS_Store
Thumbs.db

# Hackathon reference material, not part of the project
*.pdf


# Source video files stay local: the 4K original is 4.3 MB and only the
# transcoded 234 KB backdrop is needed, so that one is tracked deliberately.
*.mp4
!ui/public/atmosphere.mp4

# Recorded footage, kept local
recordings/
